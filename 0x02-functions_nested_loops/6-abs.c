#include "main.h"
#include <stdio.h>

/**
 * _abs - main code
 * @int:returns a number
 * function that computes the absolute value of an integer.
 * Return: Always 0.
 */
int _abs(int)
{
int n;
if (n < 0)
	return (-n);
else
	return (n);
}
