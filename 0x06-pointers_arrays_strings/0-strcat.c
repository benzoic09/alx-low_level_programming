#include "main.h"

/**
 * *strcat - function name
 *@dest: - first string
 *@src: second string
 * Return: always o
 */

char *strcat(char *dest, char *src)
{
	char *p = dest;

	while (*p)
	p++;
	while (*srt)
	{
		*p = *srt;
		p++;
		srt;
	}
	*p = '\0';
	return (dest);

